.cache.json*
.history.db
.history.db-wal
.history.db-shm
lib/
.refresh.lock
.force.stamp
.place.stamp
.place.lock
.siren.stamp
.gaz.stamp
.hist.stamp
.cam/
.cams.json*
.prf.json
shots/
.superpowers/
.php-error.log
.client-errors.log
.watch.state
.claude/settings.local.json

# The build output. `node build.mjs` writes it, and a deploy publishes it.
site/
node_modules/

# The Playwright MCP server writes a console log and an accessibility tree here, with a session
# timestamp in each file name. It writes new ones on every session, so the directory grows for ever.
.playwright-mcp/
